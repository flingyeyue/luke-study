# AI 使用记录

本文记录 AI 对可视化数据转换流水线项目的实际贡献，以及人工决策、修正和验证结果。

## 项目信息

- 项目：可视化数据转换流水线
- 代码目录：`/home/lucas/code/luke/project`
- 知识库：`/home/lucas/code/luke/study/topics/visual-data-pipeline`
- AI 工具：Codex
- 人工负责人：待填写

## 活动记录

### 2026-07-11：确定项目并建立开发知识库

- 目标：将“不依赖数据库的可视化数据转换流水线”拆解为可以由多个 Codex 终端并行开发的计划。
- AI 贡献：提出 MVP 边界、纯浏览器架构、共享契约、12 个工作日阶段安排、六终端所有权和任务依赖；建立产品、架构、契约、计划、测试和决策文档。
- 提示/任务摘要：用户选择可视化数据转换流水线，要求详细开发计划、任务拆分，并在当前目录的 `study/` 维护知识库。
- 产出：本目录中的项目知识库文档。
- 人工决策：用户确认项目方向，并指定 `study/` 为知识库位置；技术细节和任务边界仍需用户审阅。
- 验证：已确认 `study/` 为现有独立 Git 知识库，`project/` 为空的独立 Git 仓库；`git diff --check` 未发现空白错误；知识库入口列出的 8 个相对链接均对应已创建文件。
- 问题与修正：尚未发生实现错误；计划中的时间和性能数字均明确标记为目标或估计，不作为真实成效。
- 证据：`README.md`、`01-product-scope.md` 至 `06-testing-and-acceptance.md`、`decisions.md`。

### 2026-07-11：确立知识库提交规则

- 目标：确保知识库每次更新都有可追踪版本。
- AI 贡献：将用户要求转化为知识库维护规范，并准备首次提交。
- 提示/任务摘要：每次完善知识库后都需要 Git 提交。
- 产出：更新项目知识库入口中的维护规则。
- 人工决策：用户明确要求每次知识库更新后提交。
- 验证：提交前检查 Markdown 和 Git 状态；提交完成后在交付信息中报告实际提交哈希。
- 问题与修正：无。
- 证据：本文件及项目知识库 `README.md`。

### 2026-07-11：建立受限的知识库 Git Skill

- 目标：复用知识库的 Deploy Key 提交与推送流程，并限制使用目录和目标仓库。
- AI 贡献：查询 Codex 官方项目级 Skill 发现规则，设计 `push-luke-study` 的作用域校验、变更审查、提交和远程校验流程。
- 提示/任务摘要：把当前 Git 操作做成 Skill，并确保只有在 `/home/lucas/code/luke` 及其下级目录启动的 Codex 可以使用。
- 产出：`study/.agents/skills/push-luke-study` 及当前根目录、现有嵌套仓库的发现链接。
- 人工决策：用户指定目录作用域，并已授权 `luke-study` Deploy Key 写权限。
- 验证：系统 `quick_validate.py` 校验通过；`bash -n` 语法检查通过；从 `project/` 执行检查成功；从 `/tmp` 执行被作用域校验拒绝；根目录和 `project/` 的符号链接均解析到知识库中的唯一 Skill；敏感凭据模式扫描无匹配。提交和远程推送结果在交付信息中报告。
- 问题与修正：官方手册 helper 因响应缺少校验头失败，改用 OpenAI 官方文档确认 `.agents/skills` 的仓库扫描规则。
- 证据：Skill 文件、脚本测试结果、本次提交，以及 OpenAI 官方 `https://learn.chatgpt.com/docs/build-skills` 的项目级 Skill 位置说明。

### 2026-07-11：完善技术、开发规划和任务划分

- 目标：让知识库中的技术选型、阶段计划和多终端任务可以直接指导项目开发。
- AI 贡献：补充技术栈用途、选型理由、冻结状态、版本策略和依赖准入；为 12 天计划增加六个里程碑、质量门禁、合并顺序与风险降级；为六个终端增加任务包、启动规则、提示模板和 Definition of Done。
- 提示/任务摘要：在知识库写清项目使用的技术、开发规划和任务划分。
- 产出：更新 `02-architecture.md`、`04-development-plan.md` 和 `05-task-board.md`。
- 人工决策：用户要求这三类内容进入知识库；具体底层表格运算库、表达式库、XLSX 方案和目标浏览器仍需在原型阶段确认。
- 验证：`study-git.sh check` 和 `git diff --check` 通过；变更限定为 4 个预期文档，共新增 217 行；敏感凭据模式扫描无匹配；已确认技术栈包含待确认标记、开发计划包含 M0～M5、任务看板包含终端 0～5 和 Definition of Done。Git 提交及远程推送结果在交付信息中报告。
- 问题与修正：现有文档已有基本内容，因此选择原位补充而非新增重复文档，避免后续出现两个事实来源。
- 证据：上述三份文档、本次提交和各依赖官方文档链接。

### 2026-07-11：增加远程文件夹报告

- 目标：每次知识库提交推送后，明确告诉用户变更位于远程仓库的哪些文件夹。
- AI 贡献：更新 `push-luke-study` 的交付规则，并让推送脚本根据提交树自动计算、去重和排序远程文件夹。
- 提示/任务摘要：每次提交后报告远程文件夹，并把要求写入 Git 提交 Skill。
- 产出：更新 Skill、`study-git.sh`、知识库维护规则和 AI 使用记录。
- 人工决策：用户明确要求每次提交后都报告远程目录。
- 验证：系统 `quick_validate.py` 校验通过；`bash -n`、`study-git.sh check` 和 `git diff --check` 通过；敏感凭据模式扫描无匹配。实际远程文件夹输出和远程哈希一致性在本次推送后报告。
- 问题与修正：无。
- 证据：Skill、脚本、本次提交及推送输出。

### 2026-07-11：建立共享契约双副本

- 目标：形成可供多终端执行的完整共享契约，并确保知识库与代码仓库副本持续一致。
- AI 贡献：定义项目格式、节点配置、端口、数据批次、表达式 AST、数据源绑定、执行状态、Worker 消息、诊断、安全与契约测试；建立双副本校验脚本和工作区持久同步规则。
- 提示/任务摘要：在知识库和 `project` 各放一份契约，以后每次更新必须同时更新两边。
- 产出：`study/topics/visual-data-pipeline/03-contracts.md`、`project/docs/contracts.md`、`project/scripts/check-contract-sync.sh` 和工作区 `AGENTS.md`。
- 人工决策：用户明确要求契约双副本同步。
- 验证：两份契约均为 523 行，SHA-256 同为 `9e7e9162a4a0b59c0e701218cd78579090f0ab19a6da88dee2739b866a86fe54`；同步脚本和 `bash -n` 通过；两个仓库的 `git diff --check` 通过；敏感凭据模式扫描无匹配。Git 提交与远程推送结果在交付信息中报告。
- 问题与修正：原知识库契约只有概念级字段，扩展为版本化规范；公共边界中的日期改用 ISO 字符串，不跨 Worker 传递 JavaScript `Date`。
- 证据：两份契约、同步脚本、工作区规则和本次提交。

### 2026-07-11：完成 M0 工程基线

- 目标：完成 T0-01 至 T0-04，为多终端并行开发建立可重复的工程和契约基线。
- AI 贡献：初始化 pnpm workspace、React/Vite Web 壳、严格 TypeScript、ESLint、Prettier、Vitest 和 CI；实现 14 类节点配置、项目、数据、诊断与 Worker 的 Zod Schema；实现 Worker 最小处理器、虚构夹具和项目文档。
- 提示/任务摘要：按照任务计划持续执行，遇到需要新终端时再通知用户。
- 产出：`apps/web`、`packages/contracts`、`packages/test-fixtures`、根工具链、CI 和 README。
- 人工决策：没有采用 npm `latest` 的 TypeScript 7.0.2，因为 `typescript-eslint` peer range 小于 6.1；锁定 TypeScript 5.9.3。M0 Worker 只完成协议，不把未实现的节点执行描述为完成。
- 验证：`pnpm install --frozen-lockfile`、lint、typecheck、format check、build 和契约同步均通过；contracts 9 项测试和 web 6 项测试通过；Vite 开发服务器在 `http://localhost:5173/` 启动并通过 HTTP 页面和模块请求；敏感凭据模式扫描无匹配。
- 问题与修正：初次验证发现 ESLint 类型配置误作用于 JS 配置、Vite 的 test 配置类型来源错误、Vitest API 未显式导入、两个仓库使用不同 Prettier 配置导致契约漂移；分别通过限定 ESLint files、从 `vitest/config` 导入、显式导入测试 API、统一契约格式并将契约排除出普通格式化修正。收尾时曾根据短哈希写入未经验证的完整提交值，在知识库提交前通过 `git rev-parse HEAD` 发现并替换为真实值。
- 证据：项目提交 `5ea7e5157807e153a52ec8be26fc4e067e5f0cd7`、测试输出、构建产物统计和本次知识库提交。

### 2026-07-11：准备 Wave 1 并行工作区

- 目标：让五个功能终端从同一 M0 基线开始并行开发，避免共享目录写冲突。
- AI 贡献：创建 canvas、engine、file-adapters、node-config 和 qa-delivery 五个分支及 Git worktree；安装冻结依赖；建立项目级 Skill 发现链接。
- 提示/任务摘要：持续执行计划，在需要新开终端时通知用户。
- 产出：五个可直接打开的干净 worktree，均位于 `/home/lucas/code/luke` 下。
- 人工决策：用户授权持续执行，并要求在需要新终端时通知。
- 验证：五个 worktree 均位于项目提交 `5ea7e5157807e153a52ec8be26fc4e067e5f0cd7`，`pnpm install --frozen-lockfile` 成功，分支与任务所有权一致，Git 工作区干净，Skill 链接解析到知识库中的唯一版本。
- 问题与修正：首次批量状态检查的 JavaScript 工具编排字符串存在语法错误，未执行任何仓库操作；修正字符串后检查通过。
- 证据：`git worktree list`、各 worktree 的 Git 状态、依赖安装输出和任务看板。

### 2026-07-11：完成 Wave 1 并行骨架与主分支集成

- 目标：完成画布、执行引擎、文件适配器、节点面板和浏览器 QA 五个独立任务包，并将其集成为可运行的工作区骨架。
- AI 贡献：在五个 worktree 中分别实现 React Flow 画布与撤销重做、图校验与顺序调度器、CSV/JSON 解析和字段推断、配置校验与数据面板、Playwright/CI；按依赖顺序合并分支，并在主分支接通画布选中节点、配置编辑和运行数据面板。
- 提示/任务摘要：用户要求继续执行 Wave 1，仅在必须由用户操作时停止。
- 产出：画布提交 `cf21e21`、引擎提交 `d6c748a`、文件提交 `fb503b3`、节点面板提交 `91698a3`、QA 提交 `7ce9ad0`，以及主分支集成提交 `603a0ab906c5b1742bb697ef06d64c2089f61dda`。
- 人工决策：用户授权自主推进。实现中保持 M1 状态为进行中，因为现有 CSV 浏览器测试只验证文件上传读取，产品 UI 尚未串通 CSV 适配器、Worker 和真实预览。
- 验证：主分支 lint、typecheck、format check、build 和双副本契约同步通过；contracts 9 项、web 14 项、file-adapters 8 项、pipeline-engine 9 项，共 40 项单元/组件测试通过；Chromium 2 项 E2E 通过；在 1440×900 桌面和 Pixel 7 移动视口检查实际截图，画布非空白且未发现元素遮挡或文本溢出。
- 问题与修正：画布分支使用了不存在的 Lucide `FitScreen` 图标，由 typecheck 发现后改为 `Maximize2`；引擎分支修正了联合元组类型推断、重复输入优先触发导致的错误测试夹具和 mock 类型；节点分支修正 React effect 中同步 setState 的 lint 问题及只读夹具类型；QA 分支补充根 TypeScript 项目配置；合并文件适配器时仅 `pnpm-lock.yaml` 冲突，通过 pnpm 重新生成锁文件解决。集成测试发现 jsdom 缺少 `ResizeObserver`，在测试初始化中加入最小 mock；E2E 发现运行数据区域选择器与内部导航名称歧义，改用精确 region 角色。首次项目提交因仓库未配置作者身份失败，读取已有提交身份后仅在项目仓库本地配置并重试成功。
- 证据：上述六个项目提交、CI 配置、40 项测试输出、2 项 Playwright 输出、桌面与移动截图及契约同步哈希。

### 2026-07-11：建立工作起止进度报告 Skill

- 目标：让后续 Codex 在开始和结束项目工作时固定报告真实进度，并在需要用户操作时明确停止和说明阻塞。
- AI 贡献：创建项目级 `report-work-progress` Skill，定义开始报告、过程更新、结束报告、阻塞升级和知识库维护规则；建立根目录与 `project/` 的发现链接。
- 提示/任务摘要：用户要求继续开发前先写一个 Skill，规定开始工作和结束工作时输出当前进度。
- 产出：`study/.agents/skills/report-work-progress/SKILL.md` 和 `agents/openai.yaml`。
- 人工决策：用户明确要求把双阶段进度报告固化为 Skill。
- 验证：系统 `quick_validate.py` 通过；根目录与 `project/` 链接均解析到知识库中的唯一 Skill 目录。
- 问题与修正：首次初始化时中文短描述只有 20 个字符，不满足生成器要求的 25～64 字符，初始化器在生成 UI 元数据前停止；保留已创建的模板目录，完成正式内容后使用合规描述重新生成并验证。
- 证据：Skill 文件、验证输出、链接解析结果和本次知识库提交。

### 2026-07-11：完成 M1 CSV 最小垂直链路

- 目标：把 Wave 1 的 CSV 适配器、Worker 协议和预览面板接成真实可操作的产品链路。
- AI 贡献：新增 CSV 选择与运行控制器；将 Worker 协议处理器改为异步执行 CSV 输入节点、缓存分页预览并输出运行事件、节点结果和诊断；把 DataPanel 接到真实 batch、Schema、行数和日志；将原文件读取冒烟测试替换为完整应用 E2E。
- 提示/任务摘要：用户要求继续任务，并要求工作开始与结束时报告当前进度。
- 产出：项目提交 `6f2df588b6983197dbd5f9572bcfa87bb664f91b`，完成“选择 CSV → Worker 解析 → 字段推断 → 数据预览”链路。
- 人工决策：沿用共享契约已有的 `run.sources`、运行事件和 `preview` 命令，不新增私有 Worker 消息；T5-03 不标记完成，因为转换和 CSV 导出尚未实现。
- 验证：lint、typecheck、format check、build 和契约同步通过；contracts 9 项、web 17 项、file-adapters 8 项、pipeline-engine 9 项，共 43 项单元/组件测试通过；2 项 Chromium E2E 通过，其中真实业务测试上传 523 B 的固定 CSV，并断言 10 行、字段名、首末记录和 `amount` 数字类型；人工检查导入后桌面截图，未发现布局遮挡。
- 问题与修正：严格类型检查发现可选 prop、React 19 `useRef` 初值和数组索引收窄问题，逐项收紧类型；E2E 最初写错夹具大小，改用 `stat` 的真实 523 B；并行冷启动时 Vite 首次发现 Worker 内的 Papa Parse 后重载页面，通过预优化 `@luke/file-adapters > papaparse` 修复；Lint 与 Playwright 并行删除 `test-results` 发生目录扫描竞态，将生成目录加入 ESLint 忽略并改为顺序门禁；复核时补充文件读取异常诊断和异步解析后的取消检查。
- 证据：项目提交、43 项测试输出、2 项 E2E 输出、构建产物、契约同步哈希和导入后截图。

### 2026-07-11：完成 T2-03 选择与类型转换执行器

- 目标：实现 Wave 2 的选择、重命名和类型转换数据执行器，并覆盖空值和转换失败策略。
- AI 贡献：实现按稳定列 ID 的选择、排序和重命名；实现 string、number、boolean、date、datetime 转换及 `fail`、`null`、`keep-original` 策略；增加节点 kind 分派和运行时配置校验入口，诊断携带节点、字段与行号。
- 提示/任务摘要：用户要求继续执行当前项目任务。
- 产出：项目提交 `fd496df27515282f5db8cc842030b7c3fd6b4bd0`，完成 T2-03。
- 人工决策：转换层采用无副作用纯函数，不修改现有只返回运行状态的调度器接口；公共数据中的日期继续使用字符串，不引入跨 Worker 的 `Date`。
- 验证：pipeline-engine 新增 7 项转换测试，项目合计 50 项单元/组件测试和 2 项 Chromium E2E 通过；lint、typecheck、build、format 和契约同步通过。
- 问题与修正：首次日期实现使用 `Date.parse`，测试证明它会把 `2026-02-31` 自动归一化为三月；改为拆分年月日并与 UTC 解析结果逐项比对。随后 ESLint 发现一个多余非空断言，改用显式 `NaN` 默认值消除断言并保持严格校验。
- 证据：转换执行器、注册入口、测试输出、项目提交和本次知识库提交。

### 2026-07-11：完成 T2-04 筛选与计算字段执行器

- 目标：实现受限表达式筛选和计算字段，不允许执行用户代码。
- AI 贡献：实现共享契约 AST 的递归解释器，支持字面量、列引用、空值判断、逻辑、比较和有限数值运算；实现短路逻辑、严格类型检查、除零与非有限结果保护；完成筛选和计算字段执行器、字段类型推断、行级诊断和注册分派。
- 提示/任务摘要：用户要求继续执行项目任务。
- 产出：项目提交 `f83a5567d3b09d8617f92ac4b12df57f1e8d8423`，完成 T2-04。
- 人工决策：表达式只解释结构化 AST，不接收或执行 JavaScript 字符串；相等使用严格比较，大小比较只允许两个 number 或两个 string，算术只允许有限 number，计算失败返回原始批次。
- 验证：pipeline-engine 新增 8 项表达式、筛选和计算字段测试，项目合计 58 项单元/组件测试和 2 项 Chromium E2E 通过；lint、typecheck、format、build 和契约同步通过。
- 问题与修正：首次类型检查发现逻辑运算已经被前置短路分支完全收窄，末尾 switch 中保留的 `and/or` 不可达分支反而违反 TypeScript 类型约束；删除冗余分支后模块与全量验证通过。
- 证据：AST 解释器、筛选与计算执行器、测试输出、项目提交和本次知识库提交。

### 2026-07-11：完成 T2-05 排序与去重执行器

- 目标：实现稳定多字段排序和多字段去重，并明确空值及重复保留语义。
- AI 贡献：实现多规则稳定排序、独立于升降序的 nulls first/last、同类型值比较和混合类型诊断；实现带类型复合键去重、`first/last` 保留策略及保留行原始相对顺序；接入统一转换注册入口。
- 提示/任务摘要：用户要求继续下一项任务。
- 产出：项目提交 `e517d7de2881180e7dfc08f95ff683e7e5716e88`，完成 T2-05。
- 人工决策：排序不进行字符串与数字之间的隐式转换；空值位置不随 descending 反转；去重复合键编码值类型，确保数字 `1` 与字符串 `"1"` 不被视为相同键。
- 验证：pipeline-engine 新增 7 项排序和去重测试，项目合计 65 项单元/组件测试和 2 项 Chromium E2E 通过；lint、typecheck、format、build 和契约同步通过。
- 问题与修正：实现检查时发现若直接对比较结果应用 descending 反转，会同时反转空值位置；在应用方向前单独返回空值比较结果，并增加降序 nulls first 测试。一次批量补丁工具调用使用了错误变量名，工具在执行任何文件修改前失败，修正编排后重新应用。
- 证据：排序与去重执行器、注册入口、测试输出、项目提交和本次知识库提交。

### 2026-07-11：完成 T2-06 分组统计执行器

- 目标：实现 count、sum、avg、min 和 max 分组统计，完成 Wave 2 引擎核心节点组。
- AI 贡献：实现无分组全表统计、多字段类型化分组、首次出现顺序输出、五类聚合、空值忽略语义、空输入行为、输出 Schema 和稳定聚合列 ID；增加配置、缺失列、Schema 类型及运行值诊断并接入转换注册入口。
- 提示/任务摘要：用户要求继续执行下一项任务。
- 产出：项目提交 `bb2763ab390326e70741446836ffda15dd626120`，完成 T2-06；T2-03 至 T2-06 均已完成。
- 人工决策：无列 count 统计行数，有列 count 只统计非空值；sum/avg/min/max 忽略 null 且无有效值返回 null；sum/avg 只接受 number，min/max 只接受 number、string、date、datetime。
- 验证：pipeline-engine 新增 6 项分组聚合测试，项目合计 71 项单元/组件测试和 2 项 Chromium E2E 通过；lint、typecheck、format、build 和契约同步通过。
- 问题与修正：模块测试通过后复核发现不适用的聚合类型会误用 `COLUMN_NOT_FOUND` 诊断代码，改为 `AGGREGATION_CONFIG_INVALID`；同时增加 min/max 运行值类型保护，避免与 Schema 不符的 boolean 值进入比较。
- 证据：分组执行器、注册入口、测试输出、项目提交和本次知识库提交。

### 2026-07-11：连续完成 T3-03、T4-03、T4-04 和 T1-03

- 目标：不中断地推进 Wave 2 剩余独立任务，为核心链路集成准备导出、配置、预览和画布状态能力。
- AI 贡献：实现 CSV/JSON 导出和 Spreadsheet 公式注入防护；向画布开放全部核心转换节点并验证七类配置；将 1000 行预览改为固定行高窗口化渲染；实现端口定义校验、单输入占用、重复边、自连和环检测，以及 Worker 运行状态映射。
- 提示/任务摘要：用户要求完成一项后不停止，直接开始下一项。
- 产出：T3-03 提交 `51936c4fee499edb1a3a8a93492bb936c4cc9a8f`、T4-03 提交 `313a03b9b772c2129056a50ba2445a9e950e7a45`、T4-04 提交 `3df4f1c255272a8d710dbca7c57a33ca6a45c669`、T1-03 提交 `e6309b580bb2f17f3f47ce67eed6000def539fcb`。
- 人工决策：CSV 公式字符串前置单引号但负数保持数值；配置 UI 保留契约校验的 JSON 编辑方式并补齐所有核心节点入口；虚拟表只渲染可视行及 overscan；运行状态更新不进入撤销历史。
- 验证：T1-03 完成后的项目合计 87 项单元/组件测试和 2 项 Chromium E2E 通过；lint 零警告、typecheck、format、build 和契约同步通过；桌面截图确认扩展节点库无重叠。
- 问题与修正：配置参数化测试因测试环境未自动 cleanup 导致 DOM 残留，增加全局 `afterEach(cleanup)`；虚拟表严格可选类型和 Hook 稳定依赖分别由 typecheck/Lint 发现并修正；首次全量 Lint 参数多传一个 `--` 被识别为文件模式，改用正确参数；虚拟表增加“已加载行数”后 E2E 文本选择器歧义，改为精确匹配。
- 证据：四个项目提交、87 项测试输出、2 项 E2E 输出、构建产物、截图和本次知识库提交。

### 2026-07-11：完成 T5-03 核心链路 E2E 和 Wave 2

- 目标：把独立的输入、转换、预览和导出能力集成为可在真实浏览器操作的单输入流水线。
- AI 贡献：将 Worker 升级为图校验后按拓扑顺序传递 DataBatch，执行转换与聚合节点并缓存每节点预览；让 Web 预览末端节点、映射全部节点状态并下载安全 CSV；增加点击节点模板自动连接末端的快捷工作流；扩展 Playwright 覆盖导入、创建筛选/输出节点、运行、预览和下载内容。
- 提示/任务摘要：用户要求继续执行，且完成一项后直接开始下一项。
- 产出：项目提交 `c06f7502a83bd4b011f253e835ce0d2c8ae2ba1b`，完成 T5-03、Wave 2 和 M2。
- 人工决策：继续使用既有 Worker `run`/`preview` 契约，不新增导出消息；Worker 负责数据计算，主线程仅将已返回的末端预览序列化为用户下载文件；点击添加与拖放添加共用相同端口约束。
- 验证：项目共 87 项单元/组件测试和 2 项 Chromium E2E 通过；核心 E2E 创建 CSV 输入 → 筛选 → CSV 输出三节点链路，验证 10 行预览、Schema，并捕获 `pipeline-output.csv` 下载确认包含 `ORD-1010`；lint 零警告、typecheck、format、build 和契约同步通过。
- 问题与修正：首次类型检查发现重构拖放逻辑后遗留未使用的 `CanvasNodeData` 导入，删除后全量验证通过。
- 证据：Worker 图执行代码、下载流程、Playwright 下载断言、项目提交和本次知识库提交。

### 2026-07-11：连续完成 T2-07 Join 和 T2-08 缓存隔离

- 目标：进入 Wave 3 后连续完成双输入 Join 与增量缓存基础能力。
- AI 贡献：实现 inner/left Join、复合类型键、右侧重复键展开、null 不匹配、左连接补空、右字段稳定 ID 和名称冲突前缀；实现确定性节点指纹、精确缓存命中、变更节点及下游失效，并在 Web 层忽略非当前 runId 的迟到事件。
- 提示/任务摘要：用户再次明确要求完成一项后继续下一项，不要停止。
- 产出：T2-07 提交 `d14f0ccc6406f13f39c1263d5e3ee35629e8f44a`，T2-08 提交 `d3abf867d025721b2a95b114124ce35ce4587309`。
- 人工决策：Join 的 null 键不参与匹配；右侧重复键产生多行结果；缓存指纹使用按对象键排序的确定性序列化，上游指纹参与下游指纹。
- 验证：项目共 94 项单元/组件测试和 2 项 Chromium E2E 通过；lint 零警告、typecheck、format、build 和契约同步通过。
- 问题与修正：两项实现均在模块检查后直接通过全量门禁，未发现需要返工的行为缺陷。
- 证据：Join、缓存和 runId 隔离代码，测试输出、两个项目提交和本次知识库提交。

### 2026-07-12：连续完成 T3-04 XLSX 和 T3-05 项目文件

- 目标：完成 Excel 数据交换和版本化项目持久化基础。
- AI 贡献：使用 ExcelJS 实现工作表选择、日期 ISO 化、公式文本导入和安全 XLSX 导出；实现项目 JSON 保存、打开、格式版本和节点配置校验，并排除 File 与运行数据。
- 产出：T3-04 提交 `2daf8a569b45b7202481d7e33a7bfead23852e56`，T3-05 提交 `588832f9b06488e98c474f8316313e420466592e`。
- 验证：项目共 100 项单元/组件测试和 2 项 Chromium E2E 通过；lint 零警告、typecheck、build 和契约同步通过。
- 问题与修正：ExcelJS 从根入口导出导致 Web/Worker 各增加约 1 MB，改用包子路径隔离后恢复原体积；E2E 端口被其他项目占用且错误复用，改为专用 4183 并禁止复用；测试中的 Node Buffer 类型和稀疏数组 Lint 分别修正。
- 证据：XLSX 与项目文件测试、构建统计、两个项目提交和本次知识库提交。

### 2026-07-12：完成 T1-04 项目恢复和 T4-05 Join 配置体验

- 目标：补齐项目文件的用户操作闭环，并为 Join 提供明确的双输入画布和配置体验。
- AI 贡献：实现新建、打开、保存、自动草稿恢复、脏状态和离开页面保护；实现 Join 节点模板、左右输入端口与标签、键和连接策略配置，并自动把当前末端接入左端口。
- 提示/任务摘要：用户要求连续执行任务，仅在必须由用户操作时停止。
- 产出：T1-04 提交 `970823a4f7a272581ca20df975c02ac637a6c481`，T4-05 提交 `51f2d907f84f586f2d8a9659f0abf546d9bcd6d8`。
- 人工决策：草稿仅保存项目图和配置，不保存 File 或运行数据；打开项目先完整解析再原子替换画布；Join 的左右端口保持显式命名，不通过边顺序猜测输入角色。
- 验证：项目共 103 项单元/组件测试和 2 项 Chromium E2E 通过；lint 零警告、typecheck、format、build 和契约同步通过。
- 限制：本项完成 Join 引擎和配置体验，但 Worker 当前完整图执行尚未接通两个独立文件源；双输入演示必须由 T5-06 验证后才能视为完成。
- 证据：项目控制组件、Join 画布和配置测试、两个项目提交及全量门禁输出。

### 2026-07-12：完成 T5-04 十万行浏览器性能基准

- 目标：建立可重复的 100,000 行真实浏览器基准，记录耗时、JavaScript 堆和主线程长任务。
- AI 贡献：新增独立 Chromium Playwright 配置和 `test:performance` 命令；运行时生成确定性 3,171,749 B CSV，经真实文件输入和 Web Worker 解析到 1,000 行预览；使用 CDP 和 PerformanceObserver 采集指标并设置宽松回归阈值。
- 提示/任务摘要：用户要求继续执行，不在单项完成后停止。
- 产出：项目提交 `f41cccfd8f563b1cdec71100a67297db3fe96f3c`，完成 T5-04 和 Wave 3 任务列表。
- 人工决策：性能测试使用独立 4184 端口且禁止复用服务；不提交生成 CSV；门禁设置为 30 秒和 512 MiB 堆增量，用于发现严重回归，不把机器差异当作产品故障。
- 验证：最终复测为 100,000 行、452 ms、JS 堆增加 63,394,976 B（约 60.5 MiB）、1 个长任务、最长及总长任务 159 ms；103 项单元/组件测试、2 项常规 E2E 和 1 项性能 E2E 通过，lint、typecheck、format、build 和契约同步通过。
- 问题与修正：首次全量 lint 发现新增测试未纳入根 TypeScript 工程，扩展 `tsconfig.json` include 后重新执行全部门禁并通过。
- 证据：性能测试源码、`PERFORMANCE_RESULT` 输出、完整门禁结果和项目提交。

### 2026-07-12：完成 T5-05 异常和安全测试

- 目标：验证损坏文件、Spreadsheet 公式注入、运行取消和内存边界，并修复测试发现的未捕获错误。
- AI 贡献：盘点已有安全覆盖；将损坏 XLSX 的 ExcelJS 异常归一化为 `SOURCE_PARSE_FAILED`；用延迟 File 读取构造运行中取消测试，验证取消后不继续解析或发布成功结果。
- 提示/任务摘要：用户要求完成一项后继续下一项，不需要停止。
- 产出：项目提交 `efc64186ceb1054aab8f7eea8790c29fab2a8a9f`，完成 T5-05。
- 人工决策：复用既有 CSV/XLSX 导出防护测试覆盖空白前缀和 `= + - @`，数值负数保持不变；复用 T5-04 的 512 MiB 堆增量门禁作为当前内存边界证据，不重复维护另一套大文件测试。
- 验证：105 项单元/组件测试、2 项常规 Chromium E2E 和 1 项性能 E2E 通过；性能复测 100,000 行耗时 457 ms、堆增加 63,404,168 B、1 个 153 ms 长任务；lint、typecheck、format、build 和契约同步通过。
- 问题与修正：损坏 XLSX 初始实现会让 ExcelJS 拒绝 Promise 并越过统一诊断接口；增加适配器边界 catch 和精确诊断断言后修复。取消粒度仍以异步文件读取和节点边界为检查点，同步 CSV 解析中无法抢占。
- 证据：XLSX 损坏夹具、Worker 并发取消测试、既有注入测试、性能门禁输出和项目提交。

## 结果汇总

- 主要 AI 贡献：初版产品范围、架构、契约、并行计划、M0 工程基线、Wave 1 五模块实现、集成和测试策略。
- 主要人工修正与决策：待项目负责人审阅后补充。
- 已测量结果：T5-05 完成后主分支共 105 项单元/组件测试、2 项 Chromium E2E 和 1 项性能 E2E 通过；100,000 行、3,171,749 B CSV 最近复测 457 ms、JS 堆增加约 60.5 MiB、1 个 153 ms 长任务；生产构建成功。
- 限制和未解决风险：性能数据来自当前开发机 Chromium，尚未建立标准硬件基线；完整双数据源 Worker 执行和 Join 演示 E2E 尚未完成。
